spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
mail.protocol=${SPRING_MAIL_PROTOCOL}
mail.host=${SPRING_MAIL_HOST}
mail.port=${SPRING_MAIL_PORT}
mail.smtp.auth=${SPRING_MAIL_AUTH_ENABLE}
mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_TTLS_ENABLE}
mail.from=${SPRING_MAIL_FROM}
mail.username=
mail.password=


app.jwt.secret=${JWT_SECRET}

#spring.jpa.show-sql: true